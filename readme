gnome shell:
no topleft hot corner
topicons plus
dash to dock

geant4 -install dawn from source
##DEBIAN

apt-get install python3-pip python-pip
apt-get install --assume-yes libhdf5-dev
sudo apt-get install libgconf-2-4 libappindicator1 libdbusmenu-glib4 gconf-service libindicator7 libdbusmenu-gtk4 gconf2-common
sudo apt-get install gparted
sudo apt-get install hdf5-tools
sudo apt-get install clang 3.9, clang 3.9 dev
sudo apt-get install libtbb2 libtbb-dev
sudo apt-get install libgl2ps, libgl2ps-dev
sudo apt-get install libfftw3-3 libfftw3-dev
#sudo apt-get install linux-image-extra-virtual
sudo apt-get install openjdk-8-jre
sudo apt-get install cmake
sudo apt-get install  tilda
sudo apt-get install libreadline-dev git-core 
sudo apt-get install ttf-aenigma
sudo apt-get install p7zip-full 
sudo apt-get install wine
sudo apt-get install libafterimage-dev
sudo apt-get install libgfal2-dev
sudo apt-get install brasero
sudo apt-get install gedit-plugins
sudo apt-get install sysdig
sudo apt-get install handbrake
sudo apt-get install libdvd-pkg

sudo apt-get install libldap2-dev 

sudo apt-get install texlive-xetex perl-tk xzdec]

sudo apt-get install texlive-science

sudo apt-get install texlive-latex-extra

sudo apt-get install pandoc

sudo apt-get install libncurses5 libncurses5-dev --for AZURE

sudo apt-get install libreadline.dev -- for AZURE

sudo apt-get install cmake-curses-gui

sudo apt-get install libc6:i386 lib32ncurses5 libstdc++6:i386 lib32z1 libbz2-1.0:i386

sudo apt-get install libblas-dev liblapack-dev

 

sudo apt-get install pip3
sudo apt-get install dirmngr
sudo apt-get install virtualbox-5.2

sudo pip3 install jupyter metakernel zmq

pip install ipython[all]
pip install numpy scipy
pip3 install numpy scipy



ROOT:

turn python "off" if you turn python3 "on"

there was some weirdness with one of the libraries, wasn't linking properly

thisroot.sh was overriding my environment vars somehow. Still need to look into this.


Required packages:

sudo apt-get install git dpkg-dev make g++ gcc binutils libx11-dev libxpm-dev 
sudo apt-get install libxft-dev libxext-dev

 

Optional packages:
sudo apt-get install gfortran libssl-dev libpcre3-dev \
xlibmesa-glu-dev libglew1.5-dev libftgl-dev \
//default-mysql-client-dev// libfftw3-dev \
graphviz-dev libavahi-compat-libdnssd-dev \
libldap2-dev python-dev libxml2-dev libkrb5-dev \
libgsl0-dev libqt4-dev libcfitsio-dev
libtiff-dev  libgif-dev libgif7

Mesa:
install mesasdk, mesanugrid
sudo apt-get install cmake subversion perl libzstd-dev libc6-de

 
// sudo apt-get install openssh-server
// sudo apt-get install bluefish
#sudo apt-get install ttf-mscorefonts-installer // not in debian repo?

#sudo apt-get install okular okular-extra-backends okular-backend-odp okular-dev

dropbox: 
sudo apt-get install python-gtk2 python-cairo python-gobject-2 python-gi
sudo apt-get install dropbox

 
truecrypt:
sudo apt-get install dmsetup

 
virtualbox:
sudo apt-get install libqt5x11extras5 & libsdl1.2debian & libsdl-ttf2.0-0


geant4:

sudo apt-get install emacs build-essential libgsl0-dev libxerces-c-dev libcoin80-dev libglu1-mesa-dev cmake \\
libexpat1-dev libinventor1 magit libxft-dev libxml2-dev libmotif-dev \
libqt4-dev libqt4-opengl-dev tk\

ttf-mscorefonts-

.deb on trucrypt drive

sudo apt-get install cabextract libmspack0



install master pdf editor //added to script

install virtualbox

install dropbox

install google chrome



install mathematica

install insync //by hand

install truecrypt # need to copy from installs folder

 #install jpdf tweak


# just need to add desktop files

install Zotero

install Tor browser

install fiji

install fastboot and adb // maybe add these to path?


install root

install MESA # need script to change to SDK maybe

install azure

install evt2root


todo:
figure out how to automate /executables backup for different machines

add log capability to script  
copy executables folder from truecrypt drive- only 4028 and most recent mesa
add tilda etc to startup applications

install .desktop files in /usr/share/applications //didn't work, what's wrong with links?
.config?
.bashrccygnus -> .bashrc - my lab comp?
.bashrcRCrB->.bashrc, laptop
.bashrc3?? -lab 162 computational

connect bluetooth headphones
correct virtualbox users
correct execute from hdd

notes:
activating windows 7 virtual machine was a pita, had to run 


multiarch library errors for GSL in /usr/lib vs /usr/lib/x86_64-linux-gnu:
libgsl.so
libgslcblas.so
libgobject-2.0.so.0


nucnet_tools:

xsltproc
sudo apt-get install libboost-all-dev

installed aptik & unity tweak tool


Re: Unable to run shell script

    Quote Originally Posted by probono View Post
    Here is a somewhat hackish way to patch away the "showexec" option from the automounter:

    Code:

    sudo sed -i -e 's|showexec|\x00\x00\x00\x00\x00\x00\x00\x00|g' /usr/lib/udisks/udisks-daemon

    CAUTION: Works for me after a reboot, but no guarantees. Make a backup before you do this!
    
    
    https://ubuntuforums.org/showthread.php?t=2321663


TRUECRYPT ERROR
stack overflow:


	

    selected the container to mount as usual clicking on the "Select file" button
    once selected the container file hit I clicked on the "Mount" button
    in the password input popup I clicked on the "Option" button
    in the full option popup I checked the radio box "Do no mount" in the filesystem frame at the bottom of the page
    type the password and clicked on the "Ok" button
    at the shell prompt I issued the following command: sudo ntfsfix /dev/mapper/truecrypt9 please note that the proper device depends on the slot selected for the container mount (truecrypt9 is for slot 9) and this was the output of the command:

Mounting volume... $MFTMirr does not match $MFT (record 0).

FAILED

Attempting to correct errors...

Processing $MFT and $MFTMirr...

Reading $MFT... OK

Reading $MFTMirr... OK

Comparing $MFTMirr to $MFT... FAILED

Correcting differences in $MFTMirr record 0...OK

Processing of $MFT and $MFTMirr completed successfully.

Setting required flags on partition... OK

Going to empty the journal ($LogFile)... OK

NTFS volume version is 3.1.

NTFS partition /dev/mapper/truecrypt9 was processed successfully.

    went back to truecrypt window unmounted the container and mounted as usual and it worked

Virtual Box:

Top

Re: USB not working on Ubuntu 12.04 LTS (Precise)

Postby Perryg » 15. May 2012, 23:53
In a host terminal run this:

Code: Select all   Expand view
    sudo usermod -aG vboxusers <your username>


Replace <your username> with the name you use to log in. Type your user password when prompted.


Be sure to log off and back in to finalize the change in permissions
